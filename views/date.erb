<!doctype html>

<html>

  <head>
  </head>

  <body>
    <h1>Today</h1>

    <% date_today = Date.today("The date is %A, %m %d, %y") %>
    <%= "<p>#{date_today.strftime("The date is %A, %B %d, %Y")}</p>" %>
  </body

</html>
